# Player placeholder assets

The MVP player, weapon, hitboxes, dodge aura, and four class skill effects are drawn at runtime by `PlayerVisual` and `PlayerCombatEffect`. No imported texture is required.
